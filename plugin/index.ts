/**
 * Legacy entrypoint — re-exports from root index.ts for backwards compatibility.
 * New installs should use the root-level index.ts via package.json "main".
 */
export { default } from "../index.ts";
